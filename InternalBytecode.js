// Placeholder file to satisfy Metro's symbolication on some environments (Windows).
// Created to avoid ENOENT errors referencing InternalBytecode.js during development.

// No runtime code required; presence of this file prevents Metro from failing to read it.

module.exports = {};
